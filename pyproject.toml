[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "auto-trading-agent"
version = "0.1.0"
description = "Risk-gated autonomous trading agent with paper and IBKR adapters"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
